# Promostaff Hunter Bot — рабочий документ разработки

> Живой конспект проекта. Обновлять при каждой заметной доработке бота, парсера или продуктовых решений.  
> Репозиторий: **PromostaffHunter_bot** (не путать с **promostaff-agency-bot** и **promostaff-bot**).

Последнее обновление: **2026-06-03**

---

## 1. Назначение бота

Telegram-бот для исполнителей (хелперы, грузчики, промо и т.д.):

1. **Парсит** вакансии из групп через **Telethon** (`user_session`).
2. **Фильтрует** по релевантности, категории, дедупликации.
3. **Рассылает** подписчикам push по выбранным категориям (aiogram polling).
4. Даёт **отклик** (deeplink в личку заказчика с черновиком анкеты).

---

## 2. Архитектура (два контура)

```
Группы Telegram
    ↓ Telethon (user_session): события + опрос каждые 5 мин
parser.py → save_vacancy → send_vacancy_to_subscribers()
    ↓ aiogram Bot (BOT_TOKEN): polling
main.py → кнопки, админка, профили, отклики
    ↓
SQLite bot_database.db  **или** PostgreSQL (`DATABASE_URL`)
```

| Компонент | Файл | Роль |
|-----------|------|------|
| UI бота | `main.py` | Команды, FSM, рассылка, админ-меню, «Мои данные» |
| Парсер | `parser.py` | Telethon, фильтры, категории, dedupe |
| Фото профиля | `profile_photos.py` | Скачивание в `user_photos/`, отправка, health-check `file_id` |
| БД | `db.py`, `db_backend.py` | SQLite локально / PostgreSQL на Bothost prod |
| Настройки | `config.py`, `.env` | Токены, список чатов по умолчанию |
| Тесты | `tests/test_*.py` | Парсер, подписка, профиль, лента |

**Важно:** один процесс на файл `user_session` — второй инстанс ломает парсер.

---

## 3. Переменные окружения (`.env`)

| Переменная | Назначение |
|------------|------------|
| `BOT_TOKEN` | Токен бота (BotFather) |
| `YOUR_USER_ID` | Telegram ID администратора |
| `API_ID`, `API_HASH` | Telethon (my.telegram.org) |
| `DATABASE_URL` | PostgreSQL (прод Bothost): `postgresql://user:pass@host:5432/dbname` — если задан, SQLite не используется |
| `DATABASE_PATH` | Явный путь к SQLite (Bothost shared: `/app/shared/bot_database.db`) |
| `SHARED_DIR` | Каталог shared volume (Bothost: `/app/shared`) |
| `SUBSCRIPTION_SUPPORT` | Контакт для оплаты подписки (например `@username`) |
| `SUBSCRIPTION_PAY_URL` | Опционально: ссылка на оплату (когда появится) |

Сессия Telethon: файл **`user_session.session`** в корне (не коммитить).

---

## 4. Админ-меню и команды

### Кнопки reply-клавиатуры

| Кнопка | Действие |
|--------|----------|
| 📊 Статистика | Дашборд + статус парсера |
| 🔍 Ручная проверка | `/check_now` — полный прогон парсера |
| 📋 Список откликов | 10 последних откликов |
| 📝 Отчёт парсера | Debug последнего прогона |
| 👥 Список подписчиков | До 20 имён |
| 📢 Рассылка | Мастер: текст → предпросмотр → отправка |
| 🗂️ Карточки пользователей | Пагинация inline |
| 💎 Запросы Premium | Чек оплаты + ✅ Активировать 30 дн. / ❌ Отклонить |
| 🧭 Маппинг категорий | Подписчики по категориям |
| ⚠️ Жалобы | Нерешённые жалобы |
| ❓ Поддержка (админ) | Неотвеченные обращения |
| ➕ Добавить чат | FSM: ссылка → `target_chats` |
| 📋 Список чатов парсинга | Доступ Telethon к каждому чату |
| 📤 Отправить вакансию | Ручная публикация подписчикам категории |
| ❌ Закрыть меню | Убирает клавиатуру |

### Команды (без кнопки)

| Команда | Описание |
|---------|----------|
| `/admin`, `/status` | Панель / статус |
| `/check_now` | Ручной парсинг |
| `/debug_last` | Отчёт парсера |
| `/addchat`, `/removechat`, `/listchats`, `/chats` | Чаты парсинга |
| `/broadcast [текст]` | Рассылка (или через кнопку) |
| `/setplan USER_ID premium 30` | Выдать Premium на N дней |
| `/setplan USER_ID free` | Снять Premium |
| `/usercards`, `/catmap` | Карточки / маппинг |
| `/clean_old` | Удалить вакансии старше 3 дней |
| `/answer ID текст` | Ответ в поддержку |
| `/myid` | Проверка ID админа |

---

## 5. Парсер — как работает сейчас

### Потоки

1. **Старт:** `start_realtime_listener()` — Telethon + стартовый scan + плановый опрос **каждые 300 с**.
2. **Real-time:** `NewMessage` только если `chat_id` в `_monitored_chat_ids` (резолв из БД).
3. **ID вакансии:** единый `make_vacancy_id()` (md5 16 символов от `dedupe_key` или `chat_id_message_id`).

### Фильтры

- `is_helper_message()` — релевантность (глаголы найма, оплата, стоп-фразы).
- `detect_category()` — scoring **только по тексту поста**; название группы («HelpersTeam», «КиноХелпер» и т.д.) **не влияет** на категорию (в UI — только «Из чата: …»).
- Dedupe: exact по `dedupe_key`, fuzzy ~82% + телефон.
- **Окно свежести** — `is_message_recent()` (по умолчанию **36 ч**, env `VACANCY_MAX_AGE_HOURS`), не «только сегодня» МСК.

### Устойчивость парсера

- **Reconnect:** при обрыве Telethon переподключается каждые 30 с.
- **Health loop:** каждые 10 мин — проверка online и `monitored vs active_chats`; алерт админу не чаще 1 раз/час; авто-`refresh_monitored_chat_ids`.
- **Session lock:** `session_lock.py` — второй процесс на `user_session` не стартует.
- **check_now / get_new_messages:** только shared client, без временного второго Telethon.

### Задержка push (важно для продукта)

**Два канала доставки:**

1. **Realtime** — `events.NewMessage` в Telethon (должен быть почти сразу после поста в канале).
2. **Резерв** — полный проход по чатам каждые **300 с** (`PARSER_POLL_INTERVAL_SEC`), до **120** сообщений на чат.

**Почему в чате «пачками» и с лагом ~30–60+ мин (типичный кейс):**

- Раньше парсер **ждал** окончания `send_vacancy_to_subscribers` (1 с × число Premium-подписчиков на каждую вакансию) **в том же потоке**, что и разбор канала → очередь из десятков постов копилась.
- Плановый скан мог отдать **пачку** вакансий разом после прохода по всем чатам.
- В карточке **«Опубликовано»** — время поста в UTC; в Telegram-канале отображается **МСК** (+3 ч), из-за этого кажется «бот опоздал на 3 часа».

**С 2026-06-04:** push через `schedule_vacancy_push` (фон, до 2 параллельных рассылок); в UI — **«… МСК»**.

**Проверка на проде:** в логах `⚡ Парсер групп: новое сообщение chat_id=…` — realtime; `🔄 Плановая проверка: новых вакансий N` — каждые ~5 мин (даже при N=0). Если только `Got difference for channel` без `⚡` — опора на плановый incremental-скан.

**Bothost / session:** без `user_session.session` парсер **не работает** (бот отвечает, вакансии не копятся). Файл: `/app/shared/user_session.session` или `/app/user_session.session`. Первая строка в логах: `Telethon session: …` — путь должен существовать.

### Фильтр метро (Premium)

- Поле `subscribers.metro_zones` (через запятую).
- Кнопка **📍 Мои районы** — push и лента фильтруются по станциям; если в вакансии метро не указано — не отсекаем.

### Известные ограничения парсера

- [ ] Категории по ключевым словам — scoring, без ML; digest-посты «1. … 2. …» берут роль из первого блока с явной вакансией.
- [x] Закрытие вакансии — reply «закрыто», строка «ЗАКРЫТО» в посте, зачёркивание (MessageEdited), без публикации закрытых постов.

**Классификация (2026-06-05):** «координатор свадеб/организатор» ≠ супервайзер; отдельно «промо», «выгрузка/фасовка/рохля» → грузчик; без оплаты — не в ленту; fuzzy-dedupe ловит повторы одной кампании с разными адресами.

---

## 6. Пользовательское меню

| Кнопка | Действие |
|--------|----------|
| 🔍 Посмотреть новые вакансии | Меню категорий с счётчиками → лента (per-user, не глобальный `is_sent`) |
| 📨 Мои отклики | История откликов с ссылками на вакансию и заказчика |
| ⚙️ Настройки | Категории вакансий (inline) + «🔕 Отключить рассылку» |
| 📍 Мои районы | Фильтр метро (Premium) |
| 💎 Подписка | Тариф, продление, оплата |
| 👤 Мои данные | Просмотр и редактирование анкеты (ФИО, телефон, возраст, фото, доп. текст) |
| 📖 Как пользоваться | Инструкция (/help) |
| ❓ Поддержка | FSM → обращение админу |

> Совместимость: «📋 Категории», «📞 Мои контакты», «❌ Отписаться» — старые тексты кнопок по-прежнему обрабатываются.

### UX-пакет меню + профиль (2026-06-03) — реализовано

1. **⚙️ Настройки** вместо «Категории»; отключение рассылки — inline «🔕 Отключить рассылку» (сброс категорий, профиль сохраняется).
2. **👤 Мои данные** — редактирование полей, `resume_extra`, пересборка `questionnaire`.
3. **Фото на диске** — `profile_photos.py`, `SHARED_DIR/user_photos/{user_id}.jpg`, фоновая проверка `file_id`.

### UX — следующий этап

- [ ] **Premium-портфолио** — несколько фото + альбом при отклике (как Юду/Авито).
- [ ] Счётчик на кнопке ленты вакансий.
- [ ] Напоминание за 3 дня до конца Premium (cron).
- [ ] Статусы отклика (`sent` / `closed`) в UI.
- [ ] Избранное ⭐, пауза push (Premium).

---

## 7. Подписка — продуктовое решение (зафиксировано)

> **Полное описание:** [SUBSCRIPTION.md](SUBSCRIPTION.md) — тарифы, статусы запросов, FSM чека, кнопки админа.

### Текущая реализация в коде

| Элемент | Статус |
|---------|--------|
| Поля БД `plan`, `paid_until` | ✅ есть |
| `is_user_premium()` | ✅ plan=premium и paid_until не истёк |
| Free: лимит **3 категории** | ✅ |
| Premium: без лимита категорий | ✅ |
| Экран «💎 Подписка» | ✅ реквизиты, «Запросить Premium» |
| FSM «📎 Пришлите скрин чека» (фото/PDF) | ✅ 2026-06-04 |
| «💎 Запросы Premium» — чек + ✅/❌ кнопки | ✅ 2026-06-04 |
| Продление Premium (UI + extend `paid_until`) | ✅ пакет 2026-06-04 |
| Админ `/setplan` | ✅ ручная выдача; продление суммирует дни |
| Push только Premium | ✅ Free — лента без push |
| **Пробный период** | ✅ `TRIAL_DAYS` (default 7), `trial_used` |
| Фильтр метро | ✅ Premium, `metro_zones` |
| Автооплата / webhook | ❌ нет (ручная карта) |

### Решение владельца (2026-06-03)

1. **Пробный период** — дать новым пользователям полный Premium на ограниченный срок (предложение: **7 дней**, уточнить при реализации).
2. **Оплата пока вручную** — перевод на карту, без платёжного шлюза на первом этапе.
3. **Автоматизацию** (Stars / ЮKassa / T-Bank) — отложить до появления инструмента.

### UX оплаты (актуально)

```
Пользователь: 💎 Подписка → 📩 Запросить Premium / продление
  → перевод по реквизитам (SUBSCRIPTION_CARD_HINT)
  → 📎 скрин чека (фото или PDF) в боте
  → status pending → алерт админу с чеком

Админ: 💎 Запросы Premium
  → ✅ Активировать 30 дн.  /  ❌ Отклонить
  → или /setplan USER_ID premium 30 (как раньше)
```

Подробнее: [SUBSCRIPTION.md](SUBSCRIPTION.md).

### Тарифы (черновик — править перед запуском монетизации)

| Тариф | Цена | Что входит |
|-------|------|------------|
| **Free** | 0 ₽ | До 3 категорий, лента «🔍 Посмотреть новые», **без push** |
| **Trial** | 0 ₽ | `TRIAL_DAYS` дней Premium (один раз на user_id) при завершении выбора категорий |
| **Premium** | env `SUBSCRIPTION_PRICE_RUB`/мес | Push, все категории, фильтр метро |

### TODO по подписке (осталось)

- [ ] Cron массовой проверки истёкших Premium (сейчас — при `/start`).
- [ ] Напоминание за 3 дня до конца Premium.
- [ ] Telegram Stars / webhook — когда появится инструмент.

### Env для ручной оплаты

```env
SUBSCRIPTION_SUPPORT=@your_support_or_link
SUBSCRIPTION_PRICE_RUB=299
SUBSCRIPTION_CARD_HINT=Сбер •••• 1234, получатель Иван И.
TRIAL_DAYS=7
VACANCY_MAX_AGE_HOURS=36
# SUBSCRIPTION_PAY_URL=   # опционально
```

---

## 7a. Парсер — бэклог (2026-06-04, после наблюдения ~6 ч на проде)

> Сначала смотрим, что парсер реально присылает; правки фильтра — по факту. Направления согласованы.

### Пропуски (false negative)

- [ ] Ключи: `персонал`, `набор`, `ищем N человек`, `на мероприятие`, `бригада`, `staff`.
- [ ] Контакты: `wa.me`, WhatsApp, не только TG/@/телефон.
- [ ] Адрес: ТЦ, павильоны, venue; полная строка после «адрес:».
- [ ] В карточку: ставка (₽/ч, ₽/смена), дата смены.

### Шум (false positive)

- [ ] Расширить `STOP_PHRASES`: «ищу работу», «резюме в лс».
- [ ] Без найма **и** без оплаты/смены — не сохранять.

### Надёжность

- [ ] Фаза в отчёте парсера (ожидание lock / резолв / скан).
- [ ] Авто-отчёт после «Ручная проверка».

---

## 8. Антиблокировка (обязательно соблюдать)

### Bot API

- Push-вакансии: пауза **~1 с** между получателями (`SEND_DELAY`).
- Массовая рассылка: **~0.08 с** + retry на FloodWait (`BROADCAST_DELAY`).
- Не слать заблокировавшим бота; учитывать `is_active`.
- Кнопка «Отписаться».

### Telethon

- **Один** процесс на `user_session` — `session_lock.py`, алерт при втором инстансе.
- Аккаунт парсера — член групп, без спама.

---

## 9. История критичных исправлений (не забыть)

| Проблема | Решение |
|----------|---------|
| Задержка вакансий ~3 ч | Резолв chat_id, плановый опрос 5 мин, shared Telethon |
| Разные ID вакансии в БД и кнопках | `make_vacancy_id()` везде + миграция legacy ID при старте |
| `respond_add_` ломался | Фильтр callback exclude `respond_add_` |
| Закрытие вакансий не уведомляло | `mark_vacancy_closed` по message_link + notify в realtime/scan |
| Категория упаковщик → parking | Переписан `detect_category` (scoring + tie-break) |
| Free получал push как Premium | Push только `is_user_premium()` |
| Второй процесс ломал Telethon | `session_lock.py` |
| Парсер offline без алертов | Health loop + auto-reconnect |
| «Только сегодня» отсекало вечер | `is_message_recent` 36 ч |
| Нет фильтра метро | `metro_zones` + 📍 Мои районы |
| Telethon EOF на сервере | `create_authorized_client()` — connect без input(); алерт если нет `.session` |
| Конфликт `cat_` postvacancy vs регистрация | Префикс `postcat_` для админ-публикации |

---

## 10. Бэклог доработок (приоритет)

### Высокий — без этого бот «слабый»

- [x] **Пробный период** (§7).
- [x] **Реквизиты и текст оплаты** на экране подписки + «Запросить Premium».
- [x] **FSM чека оплаты** + кнопки ✅/❌ в «💎 Запросы Premium» ([SUBSCRIPTION.md](SUBSCRIPTION.md), 2026-06-04).
- [x] **Мои отклики**, **единый экран категорий**, **продление Premium** (§6, 2026-06-04).
- [x] Фильтр **метро/район** (Premium).
- [x] Уведомление при **истечении Premium** (при `/start`).
- [x] Алерт админу если парсер offline / `monitored < active_chats`.

### Средний

- [x] Окно вакансий **36 ч** (`VACANCY_MAX_AGE_HOURS`).
- [ ] Scoring вакансий вместо бинарного фильтра.
- [ ] Кнопка «Не интересно» → обучение/статистика.
- [ ] Telegram Stars или webhook оплаты (когда решите инструмент).
- [ ] `set_my_commands` для пользователя и админа.

### Низкий

- [ ] Inline «Ответить» на жалобы/поддержку.
- [ ] Экспорт подписчиков CSV.
- [ ] PostgreSQL вместо SQLite при росте — **реализовано** (`db_backend.py`, env `DATABASE_URL`); включить на Bothost prod при готовности managed DB.

---

## 11. Деплой и проверки

1. Локально: `python -m pytest tests/ -q`
2. **Telethon-сессия (обязательно на проде):**
   - Локально один раз: запустить бота → создаётся `user_session.session`
   - Аккаунт парсера — **участник** всех групп
   - **Bothost ([bothost.ru](https://bothost.ru/)):** включить «Общее хранилище» у бота → редеплой → загрузить `user_session.session` в «Общие файлы» → путь `/app/shared/user_session.session` (авто, см. `SHARED_DIR`)
   - **Timeweb / свой VPS:** volume или `/app/user_session.session`
   - Env: `API_ID`, `API_HASH`; при нестандартном пути: `TELEGRAM_SESSION_NAME=/app/shared/user_session`
   - Без `.session` парсер не стартует (бот для пользователей работает)
3. **База данных:**
   - **Локально / dev:** SQLite (`bot_database.db` в корне проекта или `DATABASE_PATH`).
   - **Bothost prod (рекомендуется):** **PostgreSQL** через managed DB на тарифе Basic/Pro+ — устойчивее при redeploy и росте подписчиков.
   - **Bothost prod (SQLite):** «Общее хранилище» включено → БД в `/app/shared/bot_database.db` (не затирается при git-deploy).
   - Перед деплоем: бэкап БД (файл или pg_dump).
   - Env SQLite: `DATABASE_PATH=/app/shared/bot_database.db`
   - Env PostgreSQL:
     ```env
     DATABASE_URL=postgresql://user:password@host.example.com:5432/promostaff_hunter
     ```
     При первом старте `init_db()` создаст таблицы и засеет категории/чаты автоматически.
4. На сервере: **один** процесс бота, `user_session.session` на месте
5. После выката: `/start` → **📊 Статистика** — строка парсера ✅, не «нет файла session»
6. **📋 Список чатов парсинга** — все активные группы ✅
7. Тестовая вакансия: push → «Откликнуться» → deeplink

### 11.1 SQLite vs PostgreSQL (Hunter)

| | SQLite в `/app/shared` | PostgreSQL (`DATABASE_URL`) |
|---|------------------------|----------------------------|
| Сложность | Работает из коробки | Managed DB на Bothost + env |
| Bothost | Общее хранилище на тарифе | Managed PostgreSQL (Basic/Pro+) |
| Масштаб | До тысяч подписчиков — норм | **Рекомендуется для prod Bothost** — не теряется при redeploy, проще бэкапы |
| Локально | **По умолчанию** (без `DATABASE_URL`) | Опционально для проверки prod-схемы |

Код поддерживает оба бэкенда через `db_backend.py`. Без `DATABASE_URL` — SQLite как раньше.

---

## 12. Связанные репозитории

| Репо | Связь |
|------|-------|
| **promostaff-agency-bot** | Основной бот агентства (ERP, смены) — другой продукт |
| **promostaff-bot** | Веб, лендинг, кабинет |
| **telegram-order-parser** | Смежный опыт парсинга заказов |

---

## 13. Журнал изменений документа

| Дата | Что добавлено |
|------|----------------|
| 2026-06-04 | Dual-backend БД: `db_backend.py`, PostgreSQL на Bothost prod (`DATABASE_URL`) |
| 2026-06-03 | Закрыты слабые места: health/reconnect, session lock, 36h window, metro, premium push, trial |
| 2026-06-03 | Первая версия: архитектура, админка, парсер, подписка (trial + ручная карта), бэклог |

---

*При доработке бота: обновите §9 (если фикс важный), §10 (закройте пункт или добавьте новый), §13 (дата + строка).*
